const user = {
    name: "John",
    email: "[EMAIL_ADDRESS]",
    isActive: true
};
function createUser({ name, isPaid }) { }
createUser({ name: "John", isPaid: true });
function createUser2(user) { }
export {};
