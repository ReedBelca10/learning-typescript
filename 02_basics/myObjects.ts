const user = {
    name: "John",
    email: "[EMAIL_ADDRESS]",
    isActive: true
}

function createUser({ name, isPaid }: { name: string, isPaid: boolean }) { }
createUser({ name: "John", isPaid: true });



export { }