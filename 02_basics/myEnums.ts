// Enums are a way to define a set of named constants. They can be numeric or string values. Enums are useful when you have a fixed set of values that you want to represent as a type.
const enum SeatChoice {
    AISLE = "AISLE",
    MIDDLE = "MIDDLE",
    WINDOW = "WINDOW"
}

// Enums can be used to define a set of named constants. They can be numeric or string values. Enums are useful when you have a fixed set of values that you want to represent as a type.
const hcSeat = SeatChoice.MIDDLE

export { }